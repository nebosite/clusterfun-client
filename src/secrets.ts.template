// Copy this file to secrets.ts, which is gitignored, and put your real ids there.
//
// ClusterFun reports to ONE GA4 property: the game is a parameter on each event
// (see libs/telemetry/AnalyticsTypes.ts), not a property of its own.  So the
// entry that matters is DEFAULT - everything routes through it.
//
// Without a secrets.ts the app still runs; it just logs analytics to the
// console instead of sending them.

export const googleTrackingIds = [
  // { name: "DEFAULT", trackingId: "G-XXXXXXXXXX" },
];
